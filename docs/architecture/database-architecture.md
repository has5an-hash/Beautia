# Database architecture

One `beautia_<demo>` database belongs to each demo. The project handoff states the datasets should contain only that demo's content and should never rely on front-end filtering to hide shared content. Import and URL replacement details are in `database/README.md`.
