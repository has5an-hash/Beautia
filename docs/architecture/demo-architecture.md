# Demo architecture

The intended demos are Nail, Clinic, Hair, Spa, Lashes, Makeup and Barber. Each must have its own database, configured identity, content, images, menus, schedules and demo configuration, while using the shared source in `core/`.

This migration has seven SQL datasets but only one supplied full WordPress archive (Nail). The `demos/` folders are inventory markers, not complete runnable packages.
