# Project handoff

The authoritative Persian handoff is `reference/PROJECT-HANDOFF-FA.md`; this short file is navigation, not a replacement.

Beautia is a premium RTL WordPress beauty-business theme with seven independently configured demos. The shared theme and companion plugin live in `core/`. The demo content configuration lives in seven isolated SQL files. Do not use legacy local paths as a runtime dependency.

Open tasks and evidence boundaries are stated in `CURRENT-STATUS.md`, `docs/qa/QA-STATUS.md`, and `AGENT-CONTINUATION-PROMPT.md`.
